services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: e-library

    ports:
      - "127.0.0.1:3000:3000"

    env_file:
      - .env

    restart: unless-stopped
